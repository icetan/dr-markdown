{
    "name": "dr-markdown"
  , "version": "0.0.1"
  , "author": {
      "name": "Christopher Fredén"
  }
  , "description": "Markdown editor based on CodeMirror and Showdown"
  , "repository": {
      "type": "git"
    , "url": "https://github.com/icetan/dr-markdown.git"
  }
  , "licenses": [
      { "type": "MIT" }
  ]
  , "devDependencies": {
      "showdown": "~0.2.0"
  }
  , "scripts": {
      "build": "boiler app.js > script.js && lessc css/style.less > css/style.css",
      "watch": "npm run-script build && kckr -e \"npm run-script build\" -r \".*\\.(coffee|less)\" ."
  }
  , "bin": {
      "dr-markdown": "./bin/dr-markdown"
  }
}
